#pragma once

#include "../attributes.hh"

namespace polymesh
{
/**
 * Datatype for disjoint subsets (union-find datastructure)
 *
 * Usage:
 *
 * TODO!
 */
/* template<class index_t>
struct disjoint_set
{
public:
    int size() { return }

private:
    index_t mParent;
    int mSize = 0;
};
*/
}
